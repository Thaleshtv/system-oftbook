# Exemplo de arquivo de variáveis de ambiente para Docker
# Copie este arquivo para .env.local ou .env.production conforme necessário

# URL do backend (ajuste conforme seu ambiente)
VITE_BACKEND_URL=http://localhost:8080/

# Configurações específicas para Docker
NODE_ENV=production
VITE_HOST=0.0.0.0
VITE_PORT=5173

# Configurações de build (opcionais)
NODE_OPTIONS=--max-old-space-size=8192

# Configurações de proxy (se necessário)
# HTTPS_PROXY=http://proxy.empresa.com:8080
# HTTP_PROXY=http://proxy.empresa.com:8080
# NO_PROXY=localhost,127.0.0.1